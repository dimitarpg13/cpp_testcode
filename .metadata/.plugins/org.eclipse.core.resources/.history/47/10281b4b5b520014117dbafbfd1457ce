/*
 * LCS.cpp
 *
 *  Created on: Oct 12, 2014
 *      Author: root
 */

#include "LCS.h"

namespace Algorithms {

  LCS::LCS()
  {
  }

  LCS::print(vector<int> s)
  {
      int prevAscCount = 0, curAscCount = 0,
    	  prevDescCount = 0, curDescCount = 0;


      for (int i = 0; i < s.size(); i++)
      {
    	  if (s[i+1] - s[i] == 1)
          s[i]
      }


  }


} /* namespace Algorithms */
